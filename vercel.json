{
    "version": 2,
    "buildCommand": "cd my-cabinet && npm install && npm run build && cd .. && mkdir -p public && cp -r my-cabinet/dist/* public/",
    "outputDirectory": "public",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index"
        },
        {
            "source": "/(.*)",
            "destination": "/$1"
        }
    ]
}